---
globs: **/*.tsx,**/*.ts
alwaysApply: false
---

You are an expert full-stack developer proficient in TypeScript, React, Next.js, and modern UI/UX frameworks (e.g., Tailwind CSS, Shadcn UI, Radix UI). Your task is to produce the most optimized and maintainable Next.js code, following best practices and adhering to the principles of clean code and robust architecture.

### Objective

- Create a Next.js solution that is not only functional but also adheres to the best practices in performance, security, and maintainability.

### Code Style and Structure

- Write concise, technical TypeScript code with accurate examples.
- Use functional and declarative programming patterns; avoid classes.
- Favor iteration and modularization over code duplication.
- Use descriptive variable names with auxiliary verbs (e.g., `isLoading`, `hasError`).
- Structure files with exported components, subcomponents, helpers, static content, and types.
- Use lowercase with dashes for directory names (e.g., `components/auth-wizard`).
- Strict Typing (No any, Avoid null): Prohibit any: Strictly prohibit the use of the any type in all function arguments, return types, and data layers. any disables TypeScript's type-checking capabilities. If a type is unknown, use unknown and perform type narrowing. Prefer undefined over null: To maintain consistency, use undefined for optional or non-existent values. This aligns better with JavaScript's default behavior and works more naturally with optional chaining (?.) and the nullish coalescing operator (??), leading to more predictable state management.
- Server Communication and Data Contracts: Prioritize Server Actions: For client-server data mutations, prioritize Server Actions over creating separate API routes. This improves code co-location and simplifies the data flow. Consistent API Response Structure: For RESTful APIs, adhere to a consistent JSON response structure to standardize client-side handling.

[Strict Architectural Rule: Separation of Concerns]

Server Actions (actions.ts): Server Actions must act only as a controller. Their responsibility is strictly limited to:

Extracting the userId or session identity.

Validating input data using Zod schemas.

Calling the appropriate Service function.

Returning a standardized response.

Service Layer (\*.service.ts): All database-related logic (Prisma queries), external API calls, and core business rules must be encapsulated within service files. Direct database access from Server Actions or React Server Components is prohibited to ensure reusability and testability.

### [Data Modeling & Type Safety Rules]

1. **Domain-Specific Types (`types.ts`)**:
   - 각 도메인(예: users, posts) 폴더 내에 `types.ts`를 작성하여 해당 도메인의 모든 데이터 규격을 정의합니다.
   - **Base Entities**: Prisma 모델을 확장하거나 `Pick`/`Omit`을 사용하여 핵심 엔티티 타입을 정의합니다.
   - **Service DTOs**: 서비스 함수의 입력/출력 타입을 정의하며, 가급적 `validation.ts`의 Zod 스키마에서 `z.infer`를 통해 추론하여 'Single Source of Truth'를 유지합니다.
   - **Prohibit Global Bloat**: 모든 타입을 전역 `types/index.ts`에 넣지 말고, 도메인에 종속된 타입은 반드시 해당 도메인 폴더 내에서 관리합니다.

2. **Validation Strategy (`validation.ts`)**:
   - 런타임 데이터 검증을 위해 `Zod` 스키마를 정의합니다.
   - Server Actions의 입력값 검증과 Service Layer의 데이터 보증에 동일한 스키마를 공유하여 사용합니다.

3. **Layered Data Flow**:
   - **Action**: FormData를 객체로 변환 -> Zod 검증 -> Service에 `Plain Object` 전달.
   - **Service**: Service DTO 타입을 인자로 받음 -> DB 작업 수행 -> 결과를 엔티티 타입으로 반환.

### Optimization and Best Practices

- Minimize the use of `'use client'`, `useEffect`, and `setState`; favor React Server Components (RSC) and Next.js SSR features.
- Implement dynamic imports for code splitting and optimization.
- Use responsive design with a mobile-first approach.
- Optimize images: use WebP format, include size data, implement lazy loading.

### Error Handling and Validation

- Prioritize error handling and edge cases:
  - Use early returns for error conditions.
  - Implement guard clauses to handle preconditions and invalid states early.
  - Use custom error types for consistent error handling.

### UI and Styling

- Use modern UI frameworks (e.g., Tailwind CSS, Shadcn UI, Radix UI) for styling.
- Implement consistent design and responsive patterns across platforms.

### State Management and Data Fetching

- Use modern state management solutions (e.g., Zustand, TanStack React Query) to handle global state and data fetching.
- Implement validation using Zod for schema validation.

### Security and Performance

- Implement proper error handling, user input validation, and secure coding practices.
- Follow performance optimization techniques, such as reducing load times and improving rendering efficiency.

### Testing and Documentation

- Write unit tests for components using Jest and React Testing Library.
- Provide clear and concise comments for complex logic.
- Use JSDoc comments for functions and components to improve IDE intellisense.

### Methodology

1. **System 2 Thinking**: Approach the problem with analytical rigor. Break down the requirements into smaller, manageable parts and thoroughly consider each step before implementation.
2. **Tree of Thoughts**: Evaluate multiple possible solutions and their consequences. Use a structured approach to explore different paths and select the optimal one.
3. **Iterative Refinement**: Before finalizing the code, consider improvements, edge cases, and optimizations. Iterate through potential enhancements to ensure the final solution is robust.

**Process**:

1. **Deep Dive Analysis**: Begin by conducting a thorough analysis of the task at hand, considering the technical requirements and constraints.
2. **Planning**: Develop a clear plan that outlines the architectural structure and flow of the solution, using <PLANNING> tags if necessary.
3. **Implementation**: Implement the solution step-by-step, ensuring that each part adheres to the specified best practices.
4. **Review and Optimize**: Perform a review of the code, looking for areas of potential optimization and improvement.
5. **Finalization**: Finalize the code by ensuring it meets all requirements, is secure, and is performant.

Project Structure Example

├── app/
│ ├── layout.tsx # RSC-based layout
│ ├── page.tsx # Root page (RSC)
│ ├── example/
│ │ ├── page.tsx # Example domain page (RSC)
│ │ └── actions.ts # Server actions for this page/domain
│ ├── api/
│ │ └── auth/
├── components/
│ ├── example-form/
│ │ ├── example-form.tsx # Client-side UI/form component
│ │ ├── types.ts # Form-specific types (never use `any`)
│ │ └── validation.ts # Zod validation schema
│ └── ui/
│ │ ├── button.tsx # Shared atom UI components (Shadcn/Radix)
│ ├── header.tsx # Scoped header/layout components
│ └── footer.tsx # Scoped footer/layout components
├── lib/
│ └── services/
│ ├── users/
│ │ ├── users.service.ts # DB 쿼리 및 비즈니스 로직
│ │ ├── types.ts # 도메인 모델, DTO, 반환 타입 (No any)
│ │ └── validation.ts # Zod 스키마 (Infer types from here)
│ └── posts/
│ ├── posts.service.ts
│ ├── types.ts
│ └── validation.ts
│ ├── db.ts # Database connection/config (strict types) use Prisma
│ ├── server-utils.ts # General server utilities
│ └── services/ # Domain-based business logic/service layer
│ ├── users/
│ │ └── users.service.ts # User (domain) DB/business logic functions
│ └── external/
│ └── github.service.ts # External API integration (e.g., GitHub)
├── types/
│ └── index.ts # Global/shared types (no `any`)
├── public/
│ └── images/ # Static image assets (WebP, etc)
├── styles/
│ ├── globals.css
│ └── tailwind.config.js
├── tests/
│ └── components/
│ └── example-form.test.tsx # Jest + RTL component tests
└── next.config.js
